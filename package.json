{
  "name": "spectest",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "api": "node server.mjs",
    "start": "node scripts/start.js",
    "dev": "node dev.mjs",
    "dev:prod": "node dev.mjs --prod",
    "build": "spec compile",
    "check": "spec check",
    "test": "vitest run",
    "test:e2e": "npx playwright test",
    "test:e2e:headed": "npx playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@spec/cli": "file:../spec/packages/cli",
    "vitest": "^1.6.1"
  }
}
